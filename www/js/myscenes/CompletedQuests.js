var CompletedQuests = (function () {
    "use strict";

    function CompletedQuests(services) {
    }

    CompletedQuests.prototype.show = function (next) {

    };

    return CompletedQuests;
})();